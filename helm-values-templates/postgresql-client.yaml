image:
  registry: ${DOCKER_REGISTRY_URL}

postgresql:
  database: G2
  file: senzing-g2/resources/schema/g2core-schema-postgresql-create.sql
  host: ${DEMO_PREFIX}-postgresql-postgresql
  password: postgres
  storageClaim: opt-senzing-claim
  username: postgres

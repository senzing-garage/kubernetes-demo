auth:
  password: passw0rd
  username: user

image:
  registry: ${DOCKER_REGISTRY_URL}
  tag: ${SENZING_DOCKER_IMAGE_VERSION_RABBITMQ}

imagePullSecrets:
  - name: ${DOCKER_REGISTRY_SECRET}

persistence:
  enabled: true
  existingClaim: rabbitmq-persistent-volume-claim

volumePermissions:
  enabled: true

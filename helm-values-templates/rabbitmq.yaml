image:
  registry: ${DOCKER_REGISTRY_URL}
  tag: 3.8.2

persistence:
  enabled: true
  existingClaim: rabbitmq-persistent-volume-claim

rabbitmq:
  password: passw0rd
  username: user

volumePermissions:
  enabled: true

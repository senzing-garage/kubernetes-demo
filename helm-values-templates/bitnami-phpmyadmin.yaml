# See https://github.com/bitnami/charts/tree/master/bitnami/phpmyadmin

image:
  registry: ${DOCKER_REGISTRY_URL}
  tag: ${SENZING_DOCKER_IMAGE_VERSION_BITNAMI_PHPMYADMIN}

db:
  host: ${DEMO_PREFIX}-mysql

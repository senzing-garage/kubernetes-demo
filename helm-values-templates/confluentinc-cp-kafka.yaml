# See https://github.com/Senzing/charts/tree/main/charts/confluentinc-cp-kafka#parameters

main:

  command:
    - sh
    - -c
    - "exec tail -f /dev/null"

  image:
    registry: ${DOCKER_REGISTRY_URL}
    tag: ${SENZING_DOCKER_IMAGE_VERSION_CONFLUENTINC_CP_KAFKA}

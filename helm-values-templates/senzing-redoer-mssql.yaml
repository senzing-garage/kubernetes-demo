# See https://github.com/Senzing/charts/tree/master/charts/senzing-redoer#parameters

main:

  image:
    registry: ${DOCKER_REGISTRY_URL}
    tag: ${SENZING_DOCKER_IMAGE_VERSION_REDOER}

  senzing:
    databaseUrl: "mssql://${DATABASE_USERNAME}:${DATABASE_PASSWORD}@${DATABASE_HOST}:1433/${DATABASE_DATABASE}"
    mountOptMicrosoft:  true
    persistentVolumeClaim: senzing-persistent-volume-claim
    subcommand: redo

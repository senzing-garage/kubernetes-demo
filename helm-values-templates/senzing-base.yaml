image:
  registry: ${DOCKER_REGISTRY_URL}
  tag: ${SENZING_DOCKER_IMAGE_VERSION_SENZING_BASE}

imagePullSecrets:
  - name: ${DOCKER_REGISTRY_SECRET}

senzing:
  args:
    - sleep
    - infinity
  persistentVolumeClaim: senzing-persistent-volume-claim

image:
  registry: ${DOCKER_REGISTRY_URL}
  tag: ${SENZING_DOCKER_IMAGE_VERSION_POSTGRES}

imagePullSecrets:
  - name: ${DOCKER_REGISTRY_SECRET}

persistence:
  enabled: false
  existingClaim: postgresql-persistent-volume-claim

postgresqlDatabase: G2
postgresqlPassword: postgres

securityContext:
  enabled: false

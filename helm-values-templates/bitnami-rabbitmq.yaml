# See https://github.com/bitnami/charts/tree/master/bitnami/rabbitmq#parameters

auth:
  password: passw0rd
  username: user

image:
  registry: ${DOCKER_REGISTRY_URL}
  tag: ${SENZING_DOCKER_IMAGE_VERSION_BITNAMI_RABBITMQ}

imagePullSecrets:
  - name: ${DOCKER_REGISTRY_SECRET}

volumePermissions:
  enabled: false
  image:
    registry: ${DOCKER_REGISTRY_URL}
    tag: ${SENZING_DOCKER_IMAGE_VERSION_BITNAMI_SHELL}

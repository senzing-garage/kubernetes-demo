image:
  registry: ${DOCKER_REGISTRY_URL}
  tag: ${SENZING_DOCKER_IMAGE_VERSION_PHPPGADMIN}

imagePullSecrets:
  - name: ${DOCKER_REGISTRY_SECRET}

ingress:
  enabled: true

phppgadmin:
  serverHost: ${DEMO_PREFIX}-postgresql

service:
  type: ClusterIP
